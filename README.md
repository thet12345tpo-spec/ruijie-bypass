# Ruijie Bypass Script

This script is for bypassing Ruijie Network Router login or brute-forcing voucher codes.

## How to run in Termux

1. **Update and upgrade Termux packages:**
   ```bash
   pkg update && pkg upgrade
   ```

2. **Install Python and Git:**
   ```bash
   pkg install python git
   ```

3. **Install required Python libraries:**
   ```bash
   pip install requests aiohttp ping3 pycryptodome
   ```

4. **Clone this repository:**
   ```bash
   git clone https://github.com/thet12345tpo-spec/ruijie-bypass.git
   cd ruijie-bypass
   ```

5. **Run the script:**
   ```bash
   python free1_fixed.py
   ```

## Note
- This script is intended for educational purposes only.
- Make sure you are connected to the Ruijie Wi-Fi network before running the script.
